name: 🌍 FloodLink GFS 1h Rainfield + Isocurves

on:
  schedule:
    - cron: "0 */1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: floodlink-gfs-rainfield
  cancel-in-progress: true

jobs:
  export-rainfield:
    runs-on: ubuntu-latest

    env:
      PYTHONUNBUFFERED: "1"

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libeccodes0

      - name: ⚙️ Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pygrib numpy matplotlib pillow

      - name: 🌧️ Generate global 1h rain time-series + isocurves
        run: |
          echo "Starting GFS 1h rainfield time-series export (with isocurves + cum + textures)..."
          python -u livefloodengine_gfs_export_rainfield.py

      - name: 💾 Commit and push updated rainfield JSON + isocurves
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin
          git pull --rebase --autostash origin main

          # Stage outputs (JSON + GeoJSON + textures folder)
          git add gfs_rain_6h.json gfs_rain_6h_cum.json gfs_rain_isolines_6h.geojson gfs_rain_6h_textures || true

          if git diff --cached --quiet; then
            echo "No rainfield / cum / isocurve / texture changes to commit."
          else
            git commit -m "🌍 Auto-update: GFS rainfield + cum + isocurves + textures ($(date -u +'%Y-%m-%dT%H:%M:%SZ'))"
            git push origin main
          fi
